import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vuecapacitorstarter.app',
  appName: 'vue-capacitor-starter',
  webDir: 'dist'
};

export default config;
